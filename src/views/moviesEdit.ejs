<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="titulo">Editar Película</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
      </figure>
      <div class="card">
        <div class="card-header d-flex justify-content-between">
            <nav aria-label ="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/movies">Peliculas</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Editando <%= movie.title %></li>
                </ol>
            </nav>
        </div>
        <div class="card-body">
             <form class="row" action="/movies/update/<%=movie.id%>?_method=PUT" method="POST">
                <div class="row">
                    <div class="col-12 col-md-6">
                        <label class="form-label" for="title">Título </label>
                        <p><input class="form-control" type="text" name="title" id="" value="<%= movie.title %>" required></p>
                    </div>
                    <div class="col-12 col-md-3"> 
                        <label class="form-label" for="rating">Rating </label>
                        <p><input class="form-control" type="number" name="rating" id="" value="<%= movie.rating %>" required></p>
                    </div>
                    <div class="col-12 col-md-3">             
                        <label class="form-label" for="awards">Premios </label>
                         <p><input class="form-control" type="number" name="awards" id="" value="<%= movie.awards %>" required></p>
                    </div>
                    <div class="col-12 col-md-3"> 
                        <label class="form-label" for="release_date">Fecha de Estreno</label>
                         <p><input class="form-control" type="date" name="release_date" id="" value="<%= moment(movie.release_date).format('YYYY-MM-DD') %>" required></p>
                    </div>
                    <div class="col-12 col-md-3"> 
                         <label class="form-label" for="length">Duración </label>
                        <p><input class="form-control" type="number" name="length" id="" value="<%= movie.length %>" required></p>
                    </div>
                    <div class="col-12 col-md-3"> 
                        <label class="form-label" for="genre_id">Género </label>
                        <p>
                        <select class="form-select" name="genre_id" id="" required>
                            <% allGenres.forEach(genre => { %>
                            <option value="<%= genre.id %>" <% if (movie.genre_id == genre.id) { %><%= 'selected' %><% } %>><%= genre.name %></option>
                            <% }); %>
                        </select>
                        </p>
                    </div>
                    <div class="col-12 col-md-3">
                        <div>
                            <label for="formFile" class="form-label">Cambiar imágen</label>
                            <input name="image" class="form-control" type="file" id="formFile" name="image">
                        </div>
                    </div>
                <div class="mb-3 col-12">
                    <div class="row">
                    <% actors.forEach((actor, i) => { %>
                            <div class="col-12 col-md-3">
                                <div class="form-check">
                                    <input name="actors" class="form-check-input" type="checkbox"<%= movie.actors.map(actor => actor.id).includes(actor.id) && "checked"  %> value="<%= actor.id %>" id="actor<%= actor.id %>">
                                    <label class="form-check-label" for="actor<%= actor.id %>">
                                        <%= actor.first_name %> <%= actor.last_name %> </label>
                                    </label>
                                </div>
                            </div>


                    <% }) %>
                </div>
            </div>
            </form>
      
            
            <div class="mt-4 mb-3 col-12">
                <button class="btn btn-outline-dark w-100">Actualizar</button>

                <form action="">

            </div>
        </form>

</body>

</html>